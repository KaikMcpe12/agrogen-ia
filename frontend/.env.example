# URL base da API REST (Spring Boot)
# Exemplo: https://api.agrogenia.com
VITE_API_URL=https://api.seubackend.com

# Define se a API real está ativa.
# "true"  → desativa os mocks, usa VITE_API_URL
# "false" → usa axios-mock-adapter (padrão de desenvolvimento)
VITE_USE_REAL_API=false
